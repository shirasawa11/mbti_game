“你现在是一位顶尖的 AVG/文字冒险游戏 UI 设计师。请根据以下要求，重构我游戏中的剧情文本排版和按钮样式。
视觉视觉风格： 黑暗奇幻、神秘、沉浸式（类似《暗黑破坏神》或《艾尔登法环》的极简高级感）。
重构要求：
引入 Google 游戏字体： 引入 Cinzel（用于英文字体/章节名）和 Noto Serif SC（思源宋体，用于中文剧情，比黑体更有故事感）。
文字阴影与对比度： 所有文字必须加上高级的软阴影（text-shadow），确保在任何背景下都绝对清晰。
重构对话框布局： 剧情文本不要零散地飘在屏幕中间。在屏幕下方设计一个半透明、带有一点点模糊效果（backdrop-filter）的游戏专用对话底框。
美化提示按钮： 重新设计‘SPACE 继续’的提示。不要突兀的蓝色块，改成古典的金色或微光的白色边框，并带有呼吸灯动画效果。

请参考以下我为你设计好的全新 UI 代码结构进行重写：

<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Cinzel:wght@500;700&family=Noto+Serif+SC:wght@400;700&display=swap" rel="stylesheet">

<style>
/* 整个游戏容器 */
.game-container {
  position: relative;
  width: 100%;
  height: 100vh;
  /* 假设这里是你的背景图 */
  background: url('your-background.jpg') center/cover no-repeat; 
  font-family: 'Noto Serif SC', serif;
  color: #e0e0e0;
  overflow: hidden;
}

/* 1. 左上角章节标题优化：拉开字距，增加史诗感 */
.chapter-title {
  position: absolute;
  top: 40px;
  left: 40px;
  font-family: 'Cinzel', 'Noto Serif SC', serif;
  font-size: 1.2rem;
  font-weight: 700;
  letter-spacing: 4px; /* 拉开字距 */
  color: #c9a054; /* 古典暗金色 */
  text-shadow: 0 2px 4px rgba(0, 0, 0, 0.8);
}

/* 2. 底部游戏对话框：解决文字看不清的核心 */
.dialogue-box {
  position: absolute;
  bottom: 50px;
  left: 50%;
  transform: translateX(-50%);
  width: 85%;
  max-width: 800px;
  min-height: 150px;
  padding: 30px 40px;
  
  /* 黑色半透明背景 + 磨砂玻璃模糊效果 */
  background: rgba(10, 15, 20, 0.75);
  backdrop-filter: blur(8px);
  -webkit-backdrop-filter: blur(8px);
  
  /* 极细的暗金色或银色边框，增加精致感 */
  border: 1px solid rgba(201, 160, 84, 0.3);
  border-radius: 4px;
  box-shadow: 0 10px 30px rgba(0, 0, 0, 0.8);
  
  box-sizing: border-box;
}

/* 剧情正文文字 */
.dialogue-text {
  font-size: 1.15rem;
  line-height: 1.8;
  letter-spacing: 2px;
  color: #f5f5f5;
  text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.6);
}

/* 3. 'SPACE 继续' 提示：优雅的古典微光风格 */
.action-prompt {
  position: absolute;
  bottom: 15px;
  right: 30px;
  display: flex;
  align-items: center;
  gap: 10px;
  font-size: 0.85rem;
  color: rgba(255, 255, 255, 0.6);
  letter-spacing: 1px;
  /* 呼吸灯动画 */
  animation: pulse 2s infinite ease-in-out;
}

/* 模拟键盘按键的样式 */
.key-badge {
  padding: 2px 8px;
  background: transparent;
  border: 1px solid rgba(255, 255, 255, 0.4);
  border-radius: 3px;
  font-family: 'Cinzel', monospace;
  font-size: 0.75rem;
  color: #fff;
  box-shadow: inset 0 0 4px rgba(255, 255, 255, 0.1);
}

/* 呼吸动画 */
@keyframes pulse {
  0%, 100% { opacity: 0.4; transform: translateY(0); }
  50% { opacity: 0.9; transform: translateY(-2px); }
}
</style>

<div class="game-container">
  <div class="chapter-title">第二章 · 迷雾</div>
  
  <div class="dialogue-box">
    <div class="dialogue-text">你进入了迷宫的更深层。四周的寒意更甚，远处的迷雾中仿佛有什么东西正在窥视着你……</div>
    
    <div class="action-prompt">
      <span class="key-badge">SPACE</span>
      <span>点击继续</span>
    </div>
  </div>
</div>